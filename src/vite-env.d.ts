/// <reference types="vite/client" />

// Injected at build time via vite.config.ts's `define` — see components/BuildBadge.tsx.
declare const __BUILD_SHA__: string;
declare const __BUILD_TIME__: string;
